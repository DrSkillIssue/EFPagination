@page
@model NullableModel

<div class="page-header">
    <h4>Nullable Column via Computed Column <code>COALESCE</code></h4>
    <p class="text-muted mb-0">Sort order: <code>NullableDateComputed ASC, Id ASC</code></p>
</div>

<partial name="_UserList" model="Model" />
