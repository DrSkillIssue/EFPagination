@page
@model IndexModel

<div class="page-header">
    <h4>Prebuilt Definition <span class="badge bg-success">Recommended</span></h4>
    <p class="text-muted mb-0">Sort order: <code>Id ASC</code></p>
</div>

<partial name="_UserList" model="Model" />
